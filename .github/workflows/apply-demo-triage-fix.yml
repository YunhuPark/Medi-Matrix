name: Apply demo triage fix

on:
  push:
    branches:
      - fix/demo-triage-differentiation
    paths:
      - .github/workflows/apply-demo-triage-fix.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/demo-triage-differentiation
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Apply targeted source patches
        run: python scripts/apply_demo_triage_fix.py
      - name: Remove one-shot patch helpers
        run: git rm scripts/apply_demo_triage_fix.py .github/workflows/apply-demo-triage-fix.yml
      - name: Commit patched sources
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: separate demo risk signals and triage fusion"
          git push origin HEAD:fix/demo-triage-differentiation
